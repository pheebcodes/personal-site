---
interface Props {
	href: string;
	class?: string;
	current?: boolean;
}

import DividedLink from "./divided-link.astro";

const { href, class: className, current } = Astro.props;
---

<DividedLink class:list={[className, { current }]} href={href}
	><slot /></DividedLink
>
